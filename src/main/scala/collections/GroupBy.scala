/*
def abc(inProgressRawTripEvents: Seq[TripEventRaw]): Iterable[TripEventContainer] = {
  inProgressRawTripEvents.groupBy(x => (x.tripNumber, x.mineId)).map {
    case ((tripNumber, mineId), tripEventRawList) =>*/
