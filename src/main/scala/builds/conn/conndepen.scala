/*
import sbt._

object Dependencies {

  val seactoreV           = "0.9.0.28-DEV-SNAPSHOT"
  //
  val seactoreIntegration = "com.seactore" %% "seactore-integration" % seactoreV

  val sqlServer = "com.microsoft.sqlserver" % "sqljdbc4" % "sqljdbc4"
  val jcifs     = "org.samba.jcifs" % "jcifs" % "1.3.3"
  val seactoreKafka = "com.seactore" %% "seactore-kafka-consumer" % seactoreV
  val vividSolutionJts = "com.vividsolutions" % "jts" % "1.13"

  val commonDeps = Seq(
    "com.seactore" %% "seactore-core" % seactoreV,
    "com.seactore" %% "seactore-http-core" % seactoreV,
    "com.seactore" %% "seactore-integration-ftp" % seactoreV,
    "com.seactore" %% "seactore-testkit" % seactoreV % "test",
    "org.apache.commons" % "commons-csv" % "1.5",
    "ch.qos.logback" % "logback-classic" % "1.1.2"
  )

  val biometricDeps = Seq(sqlServer)

  val displayDeps = Seq(jcifs)

  val integrationDependencies = Seq(seactoreIntegration)

  val sapReportDeps = Seq(seactoreKafka)

  val dispatchManagerDeps = Seq(seactoreKafka)

  val fptsDeps = Seq(seactoreKafka, vividSolutionJts)
}
*/
